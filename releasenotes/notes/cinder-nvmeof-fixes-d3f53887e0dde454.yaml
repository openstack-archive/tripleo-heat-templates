---
fixes:
  - |
    Cinder NVMe-oF: Use the right port ``4420`` instead of ``4460`` and add the
    appropriate iptables rule for LVM+nvmet to work.
  - |
    Cinder NVMe-oF: Cinder nodes where not loading ``nvme-fabrics`` kernel
    module, so nvme-of would not work correctly on controller nodes.
