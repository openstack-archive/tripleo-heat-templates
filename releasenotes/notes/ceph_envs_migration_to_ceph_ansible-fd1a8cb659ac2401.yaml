---
prelude: >
    From the Queens release the deployment of Ceph is only supported in
    containers.
deprecations:
  - |
    The pre-existing environment files which previously enabled the
    deployment of Ceph on baremetal, via puppet-ceph, will all be'
    migrated to deploy Ceph in containers, via ceph-ansible.