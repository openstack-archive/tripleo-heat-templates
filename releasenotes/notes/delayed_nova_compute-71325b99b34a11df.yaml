---
fixes:
  - |
    Delayed Nova Compute script uses `/run/nova/startup` for its state file instead
    of `/run`.
upgrade:
  - |
    Delayed Nova Compute script uses `/run/nova/startup` for its state file instead
    of `/run`.
