---
features:
  - |
    Adding ptp parameters for timemaster service configuration on overcloud
    compute node.Timemaster will use already present chrony parameters.
    PTPMessageTransport, PTPInterfaces are added new.

